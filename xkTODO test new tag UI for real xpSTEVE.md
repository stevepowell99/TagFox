# Test the new TagFox tag UI for real

The tag parsing, Everything queries and deadline date maths were verified directly, but the Electron app was not clicked through. Give it a real run:

- Deadline filter buttons: Overdue / Today / This wk / Next wk, and the X clear button.
- Date pickers in the tag editor and in Add TODO.
- Hit the tag-bar rescan (the rotate icon) once to prune any leftover remembered junk tags.

Owner: xpSTEVE (hands-on UI test).
